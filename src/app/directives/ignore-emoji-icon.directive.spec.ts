import { IgnoreEmojiIconDirective } from './ignore-emoji-icon.directive';

describe('IgnoreEmojiIconDirective', () => {
  it('should create an instance', () => {
    const directive = new IgnoreEmojiIconDirective();
    expect(directive).toBeTruthy();
  });
});
